# supportutils-plugin-suse-sap

SLES for SAP plugin for supportconfig

Instructions: 
1. Extract and copy script "suse_SAP" to /usr/lib/supportconfig/plugins directory and create the "plugins" directory if it doesn't exist. (file needs to be executable)
2. When you run the normal supportconfig, it will run any plugins that it finds in these directories.

* If you want to run minimal supportconfig but also run suse_SAP plugin script, you can run the following.
Example: 
supportconfig -i psuse_SAP

Supportconfig will create a nts_*tbz file in /var/log/ directory. 
When Supportconfig tbz file is extracted, the plugin output will be called "plugin-suse_SAP.txt"

You can run "supportconfig -F" to see list of keywords which can be used in the "-i" option above. 
Example: 
supportconfig -F
AFP APPARMOR AUDIT AUTOFS BOOT BTRFS DAEMONS CIFS CIMOM CRASH CRON DFS DHCP DISK DNS DOCKER DRBD DSFW EDIR ENV ETC EVMS HA HAPROXY HISTORY IB IMAN ISCSI KVM LDAP LUM LVM LXC MEM MOD MPIO NCP NCS NET NFS NIT NSS NTP OCFS2 OES OFILES PAM PRINT PROC PROXY SAM SAR SLERT SLP SMT SMART SMB SMS SRAID SSH SSSD SYSCONFIG SYSFS UDEV UFILES UP UPD WEB X XEN aEDIR aFSLIST aLOGS aMINDISK aMAXYAST aRPMV aSLP aLOCAL_ONLY psuse_SAP
 
Often times the "openfiles.txt and proc.txt gathering process can take awhile and the output is very large.  
You can exclude these options by using the -x keyword options. 
Example:  supportconfig -x OFILES,PROC 
** To combine the options to exclude "openfiles and proc" and to run minimal gathering plus run the suse_SAP plugin, you could run the following.
supportconfig -x OFILES,PROC -i psuse_SAP
